# Quiz Maker
This is a project I did when I started learning python and MySQL. A very simple quiz maker. 
It uses Python and MySQL :)

Python is the interface itself, and MySQL serves as the database. 

The python libraries that I used in this project were:

```
import secrets
import string
import uuid
import mysql.connector as cont

```
Except `mysql.connector` everything else were used to make authentication secure as possible with the knowledge I had that time. Everything else is simple python functions and function calls.
